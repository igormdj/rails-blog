<h1>Editar Post</h1>

<%= form_with model: @post do |form| %>
    <% if @post.errors.any? %>
        <div id="error_explanation" style="color: red; border: 1px solid red; padding: 10px; margin-bottom: 20px; background-color: #ffe6e6; border-radius: 4px;">
            <h2><%= pluralize(@post.errors.count, "erro") %> impediram este post de ser salvo:</h2>
            <ul>
                <% @post.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <%= form.label :title, "Título" %><br>
        <%= form.text_field :title %>
    </div>

    <div class="field">
        <%= form.label :body, "Conteúdo" %><br>
        <%= form.text_area :body, rows: 5 %>
    </div>

    <div class="actions">
        <%= form.submit "Atualizar Post" %>
    </div>
<% end %>

<%= link_to "Voltar para o post", post_path(@post), class: "back-link" %>
